# .env.example

# --- Firebase Admin SDK Configuration ---
# Path to your Firebase service account key file.
# This is used for server-side operations (like webhooks and secure actions).
GOOGLE_APPLICATION_CREDENTIALS=./firebase-credentials.json

# --- Database Configuration ---
# Choose one: "firebase", "mongodb", "indexeddb"
# If not set, "firebase" will be used as default.
NEXT_PUBLIC_DATABASE_TYPE=firebase

# --- MongoDB Configuration (only if using NEXT_PUBLIC_DATABASE_TYPE=mongodb) ---
MONGODB_URI="mongodb://localhost:27017"
MONGODB_DB="finwise"

# --- Default FinWise AI Provider ---
# The AI provider to use for the default "FinWise AI" option.
# Choose one: "googleai", "openai", "ollama"
DEFAULT_AI_PROVIDER=googleai

# API Keys for the default provider (only one is needed depending on the provider above)
GEMINI_API_KEY=
OPENAI_API_KEY=

# Model configuration for the default provider
DEFAULT_OPENAI_MODEL=gpt-4o
DEFAULT_OLLAMA_MODEL=llama3
DEFAULT_OLLAMA_URL="http://127.0.0.1:11434"


# --- Stripe Configuration ---
# Get your keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# --- Stripe Product Price IDs ---
# Create your products in Stripe and get the Price IDs.
# One for each plan you want to offer.
NEXT_PUBLIC_STRIPE_PRICE_ID_PRO=price_...
NEXT_PUBLIC_STRIPE_PRICE_ID_PLUS=price_...
NEXT_PUBLIC_STRIPE_PRICE_ID_INFINITY=price_...

# --- Application URL ---
# The base URL of your deployed application.
NEXT_PUBLIC_APP_URL="http://localhost:9002"
