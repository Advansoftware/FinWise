# Variáveis de Ambiente para o FinWise

# --- Configuração do Banco de Dados ---
# Escolha o adaptador de banco de dados a ser usado.
# Opções válidas: "firebase", "mongodb", "indexeddb"
# Se não for definido, "firebase" será usado como padrão.
NEXT_PUBLIC_DATABASE_TYPE=firebase

# --- Configuração do MongoDB (usado se NEXT_PUBLIC_DATABASE_TYPE=mongodb) ---
MONGODB_URI="mongodb://localhost:27017"
MONGODB_DB="finwise"

# --- Configuração do Firebase Admin (usado pelo backend e webhooks) ---
# Caminho para o arquivo JSON de credenciais da conta de serviço do Firebase Admin.
# Exemplo: "/path/to/your/serviceAccountKey.json"
GOOGLE_APPLICATION_CREDENTIALS=""

# --- Configuração de Pagamentos (Stripe) ---
# Chave secreta da API do Stripe. Encontrada no dashboard do Stripe.
STRIPE_SECRET_KEY=sk_test_...
# Segredo do Webhook do Stripe para verificar os eventos.
STRIPE_WEBHOOK_SECRET=whsec_...

# IDs dos Preços dos Planos no Stripe. Crie os produtos e preços no seu dashboard Stripe.
NEXT_PUBLIC_STRIPE_PRICE_ID_PRO=price_...
NEXT_PUBLIC_STRIPE_PRICE_ID_PLUS=price_...
NEXT_PUBLIC_STRIPE_PRICE_ID_INFINITY=price_...

# --- Configuração Geral da Aplicação ---
# URL base da sua aplicação em produção. Usado para redirecionamentos do Stripe.
NEXT_PUBLIC_APP_URL=http://localhost:9002

# --- Configuração de IA (Opcional) ---
# Chave de API para o Google AI (Gemini). Necessária para o provedor "googleai" e o padrão "finwise".
GEMINI_API_KEY=AIzaSy...
